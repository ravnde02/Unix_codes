```
#For vim editor

i = insert (edit) mode

ESC -> : -> wq      #exit insert mode, save and quit vim 

ESC -> : -> q       #exit insert mode and quit vim without saving
```
```
#Running BLUPF90 analysis

renumf90 -> parameter file name       #runs renumf90 to reformat the data 
airemlf90 <<< renf90.par              #runs reml through BLUPF90 on the parameter file
```
